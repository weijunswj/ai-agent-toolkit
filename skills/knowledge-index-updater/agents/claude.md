# Claude Notes

Copy the whole `knowledge-index-updater` skill folder into the Claude skills location supported by the target account or workspace.

Use `SKILL.md` as the agent entrypoint. Keep `agents/openai.yaml` and this note with the folder so platform-specific guidance stays local.

If Claude cannot access Notion, GitHub, or scheduled tasks, provide manual update steps and do not claim that the Knowledge Index was updated.
