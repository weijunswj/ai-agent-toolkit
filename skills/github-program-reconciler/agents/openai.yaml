interface:
  display_name: GitHub Program Reconciler
  short_description: Preflight and prove managed GitHub programme reconciliation.
  primary_color: '#1D4ED8'
  default_prompt: Use $github-program-reconciler for managed programme lifecycle inspection and mandatory preflight. Never infer mutation, Ready, merge, review-thread, owner-decision, or finality authority.
policy:
  products:
    - chatgpt
    - codex
    - api
    - atlas
  allow_implicit_invocation: true
