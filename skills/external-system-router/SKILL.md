---
name: external-system-router
description: Use for provider APIs, supported CLIs or SDKs, MCP or typed connectors, provider-console browser/computer-use, deployment, DNS, databases, storage, workflows, OAuth, external-system setup, provider capability selection, operation receipts, or supported continuation. Routes an exact owner-authorised objective through a risk-scoped envelope and the strongest admissible operation-specific interface. It is not a generic HTTP, shell, MCP, or browser executor.
---

<!--
Generated from toolkit project source. Do not edit directly.
Project: development.external-system-router
Source: _projects/development/external-system-router/_main/skill/SKILL.md
Update the project source and run sync.
-->
# External System Router

Use this skill before planning or operating against any external provider or graphical control surface.

## Required Workflow

1. Read [references/external-system-control-plane.md](references/external-system-control-plane.md) completely.
2. Reconcile repository evidence with the user's current objective. Do not install every integration during generic Toolkit setup.
   - For any material n8n task, also load `n8n-agent-rules`, classify the exact n8n operation, and create the fail-closed task ledger before workflow work.
3. Load the canonical local target registry through the bounded trusted inventory loader, then resolve one exact provider target. Reject raw/caller-created records, copied snapshots, links, replaced bytes, identity drift, generation rollback, and recomputed plans. Never guess from recent credentials, tabs, history, environment-variable order, or a generic token.
4. Build and validate the task-authorisation envelope. Task lifetime must bind the exact task ID, session fingerprint, and canonical approved-objective digest. Reuse it for in-scope API requests, CLI commands, MCP calls, and approved graphical steps; ask again only when a protected boundary changes.
5. Resolve the versioned canonical operation semantics from the authenticated snapshot. Treat caller and audit read/write fields only as assertions, reject every mismatch, and bind the same semantics digest through the envelope, audit, plan, route, approval, and receipt.
6. Audit candidate interfaces per operation and select the strongest reviewed admissible route through the loader-produced authority or its exact process-local host plan. Recompute final risk after selecting the exact audit and rerun full authorisation. Detached, copied, forged, or stale evidence cannot select a route. MCP is optional and has no global priority.
7. Before graphical control, render the complete disclosure and ask one bold explicit owner-approval question. A host, browser, operating-system, OAuth, or tool popup is technical permission, not owner approval.
8. Execute only named adapter operations. Do not expose a generic unrestricted HTTP, shell, MCP, connector, or browser path.
9. Revalidate inventory immediately before route creation and receipt creation. Verify the postcondition and write a redacted structured receipt bound to canonical mutability, final risk, selected audit, authenticated source, and precondition/result/verification/rollback evidence, with exactly one supported next action.
10. Before declaring the objective complete, mark every required capability configured and verified, explicitly deferred with a blocker, or proven unnecessary.
   - An n8n completion claim additionally requires every official Skill/helper/compiler/live-route ledger entry demanded by the exact operation.

## Safety Boundary

- Broad tool permission never expands operational authority.
- Every production mutation is at least Tier 2 and needs task-scoped explicit owner authorisation. Tier 3 operations need exact immediate approval.
- Scheduled collectors, drift checks, and advisory AI reviews remain read-only and cannot mutate providers.
- Never put secrets, tokens, cookies, connection strings, private origins, private identifiers, raw provider output, raw screenshots, customer/private payloads, or complete environment listings in Git, chat, reports, manifests, ledgers, matrices, or receipts.
- Prefer provider/connector-managed credentials, then an OS credential manager or reviewed broker, then short-lived process injection. Plaintext files and persistent environment variables are compatibility fallbacks, not vaults.
- Browser history is a last-resort discovery path only after all six safer sources fail and a bounded history disclosure receives explicit approval.
- Keep a previous working route until operation-level parity, synthetic tests, native UAT, owner-approved migration, and observation pass. Never auto-revoke credentials or delete working configuration.

## Runtime And Schemas

Use [scripts/external-system-router.cjs](scripts/external-system-router.cjs) for deterministic validation, routing, reconciliation, marker planning, receipts, and synthetic tests. Its default behavior is read-only planning.

Use [scripts/n8n-domain-router.cjs](scripts/n8n-domain-router.cjs) for mandatory n8n detection, exact operation/facet classification, official Skill source attestation, task-local capability ledgers, governed-mutation admission, and completion auditing. Existing workflows, generic validators, model knowledge, and repository tests never satisfy official-Skill evidence.

Machine-readable contracts live under [references/schemas/](references/schemas/). The consumer manifest is stable and non-secret; the target registry, capability ledger/matrix, and receipts are user-local ignored state outside Git.

## Host Notes

Read [references/host-adapters.md](references/host-adapters.md) when configuring Codex or Claude Code. Both hosts consume the same logical provider target. Technical host permissions and trust do not replace the owner task envelope.

For the optional daily ChatGPT Scheduled Task advisory, use [references/scheduled-task-handoff.md](references/scheduled-task-handoff.md). Toolkit operation does not depend on that advisory existing or running.
