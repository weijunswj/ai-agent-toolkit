# Installer MCP

The installer MCP is a future approval-gated pack installer.

It is pack-only and design-only in v1.

No arbitrary shell execution. No arbitrary file read/write. No upstream update application. No auto-merge.
