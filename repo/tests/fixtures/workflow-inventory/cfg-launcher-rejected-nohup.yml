name: CFG rejected delegator nohup
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts && nohup node repo/tests/fixtures/workflow-inventory/node-target.cjs
