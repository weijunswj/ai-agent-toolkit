name: CFG launcher command lookup capital-V
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
        with:
          node-version: '22'
      - run: command -V node repo/tests/fixtures/workflow-inventory/node-target.cjs
