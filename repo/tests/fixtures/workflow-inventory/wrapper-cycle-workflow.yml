name: Wrapper cycle
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
      - run: bash repo/tests/fixtures/workflow-inventory/wrappers/cycle-a.sh
