name: CFG launcher command dash-dash terminator
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts && command -- node repo/tests/fixtures/workflow-inventory/node-target.cjs
