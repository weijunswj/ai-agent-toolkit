name: CFG launcher nested concealment
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
        with:
          node-version: '22'
      - run: nice timeout 10 command node repo/tests/fixtures/workflow-inventory/node-target.cjs
