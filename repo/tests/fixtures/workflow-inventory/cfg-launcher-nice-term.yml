name: CFG launcher nice terminator
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts && nice -- node repo/tests/fixtures/workflow-inventory/node-target.cjs
