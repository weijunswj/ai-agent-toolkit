name: Environment safe child assignment
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
      - run: npm ci --ignore-scripts
      - run: env FIXTURE=value node repo/tests/fixtures/workflow-inventory/node-target.cjs
