name: Nested wrapper parent token oracle with PowerShell
jobs:
  fixture:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
        with:
          node-version: '22'
      - run: cd repo/tests/fixtures/workflow-inventory/packages/one && npm ci --ignore-scripts
      - run: cd repo/tests/fixtures/workflow-inventory/packages/one/src && npm run pwsh-nested
