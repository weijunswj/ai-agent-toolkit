name: JS main escape return workflow
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: ./repo/tests/fixtures/workflow-inventory/actions/local-js-main-escape-return
