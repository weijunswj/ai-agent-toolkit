name: JS pass loader workflow
jobs:
  fixture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: ./repo/tests/fixtures/workflow-inventory/actions/local-js-pass-loader
