name: CMD wrapper closure
jobs:
  fixture:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@fixture
      - uses: actions/setup-node@fixture
      - run: cmd /c repo/tests/fixtures/workflow-inventory/wrappers/hidden.cmd
