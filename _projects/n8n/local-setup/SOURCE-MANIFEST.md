# Source Manifest: Local n8n Setup

## Preserved In `_main/`

- `README.md`
- `Page 1.md`
- `Page 2.md`
- `3. tunneling guide.md`
- `3a. docker compose + ngrok.md`
- `4. vps hosting.md`
- `5. extra - claude code integration.md`
- `6. extra - opencode integration.md`
- `7. extra - antigravity integration.md`
- `templates/**`
- `scripts/**`

The local stack templates under `_main/templates/local-stack/` are toolkit-authored source for the production-shaped local Fast Path. They are not migrated upstream source pins.

## AI-Facing Surfaces

- `skills/n8n-local-setup/references/n8n/local-setup.md`, `upgrading.md`, `tunnelling.md`, `docker-compose-ngrok.md`, and `vps-hosting.md` are full guide copies generated from `_main/*.md`.
- `skills/n8n-local-setup/references/ai-agent-platforms/claude-code.md`, `opencode.md`, and `antigravity.md` are full integration guide copies generated from `_main/*.md`.
- Copied n8n setup and platform guides keep their full source bodies; source-side links to generic baseline agent-rule templates are deterministically rewritten to sibling `skills/ai-coding-agent-rules/` paths in the published skill folder for portability.
- Platform overview/router/index files that do not have preserved full source guides are generated from `curated_output_for_ai/references/`. The ChatGPT web, Claude web, and Codex platform files are short boundary-reviewed routers, not replacements for full runtime setup guides.
- Small compatibility shims generated from `curated_output_for_ai/reference-link-shims/` keep original relative links in full-fidelity copied guides resolvable inside the copyable skill folder.
- Full n8n operating rules are now owned by `_projects/development/ai-coding-agent-rules/_main/_partials/n8n-agent-rules.md` and published through `skills/n8n-agent-rules/`.
- `skills/n8n-local-setup/references/n8n-agent-rules.md` is a generated cross-skill reference copied from the canonical `n8n-agent-rules` source for portability. It is labelled as generated cross-skill content and must not be edited directly.
- Generic AI coding agent execution and toolkit skill-routing source are owned by `_projects/development/ai-coding-agent-rules/`; generic templates publish through `skills/ai-coding-agent-rules/`.
- `skills/n8n-local-setup/templates/mcp-configs/*-mcp-config.md` are generated by copy recipes from `_main/templates/*-mcp-config.md`.
- `skills/n8n-local-setup/templates/local-stack/docker-compose.yml`, `.env.example`, `n8n-local.cmd`, and `scripts/n8n-local-menu.ps1` are generated by exact copy recipes from `_main/templates/local-stack/` and provide the guided local `n8n + postgres + ngrok` Fast Path.
- AI-facing skill, MCP doc, MCP config README, and pack surfaces are generated from `curated_output_for_ai/`.
- Skill-local pack README files are short curated AI-facing sources under `curated_output_for_ai/packs/**`.
- n8n local setup no longer owns the generic AI coding agent rules, toolkit skill-routing partial, or full n8n operating ruleset.

## Excluded

- Credentials, `.env*` except placeholder-only `.env.example`, `.n8n-local/`, `.tmp/`, live n8n exports/imports, package artifacts, and private product files.
- Live workflow state. This toolkit module preserves setup documentation only.
- Full n8n agent operating rules. Use `skills/n8n-agent-rules/` for that contract.
- Legacy manual Windows ngrok helper paths are retained only as migrated source/provenance material. The supported local tunnel path is the Docker Compose ngrok service.
