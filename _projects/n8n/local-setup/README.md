# Local n8n Setup

Canonical source docs and original setup guides live in [_main/](_main/).

This module is preserved migration provenance from `weijunswj/codex-n8n-local-setup`. Source-watch is advisory/read-only, and retired internal sources are not active update targets.

Live n8n actions, credentials, imports, exports, activation, and production workflow mutation require explicit confirmation and must not run from this toolkit repo.
