# Installer MCP

The installer MCP is future design/spec-only material for a possible approval-gated pack installer.

This is MCP-ready registry and design/spec material. It is not a runnable MCP server.

No arbitrary shell execution. No arbitrary file read/write. No upstream update application. No auto-merge.
