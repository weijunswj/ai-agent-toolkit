# AI Coding Agent Rules

Project module for the published `skills/ai-coding-agent-rules/` skill.

The module owns generic AI coding agent execution rules and toolkit skill-routing rules that can be installed into Codex/OpenCode, Claude Code, Gemini CLI, or Antigravity instruction files.

See [`_main/`](_main/) for the canonical source partials and generated inert templates.
