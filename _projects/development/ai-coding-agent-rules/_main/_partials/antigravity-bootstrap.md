# Agent Toolkit Antigravity Bootstrap

<!-- AI-AGENT-TOOLKIT:BEGIN toolkit v1 -->
Root `AGENTS.md` is the canonical shared repo instruction file.

Existing Antigravity workspace rules in `.agents/rules/` remain valid and must be preserved.

**If root `AGENTS.md` is already loaded, do not duplicate, mirror, or re-import its full content.**

**If root `AGENTS.md` is not loaded or the task involves repo edits, read root `AGENTS.md` before making changes.**

**Do not overwrite or remove user-authored Antigravity rules. Only edit toolkit-managed blocks when markers are present.**

**For conflicts, the managed toolkit and n8n safety blocks in `AGENTS.md` override weaker lower instructions. Otherwise, preserve and follow the user's existing Antigravity workspace rules.**
<!-- AI-AGENT-TOOLKIT:END toolkit -->
