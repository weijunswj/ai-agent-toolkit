# Trusted CI Repository Protection

Source-only N6 material for a deterministic protected CI Gate and bounded
repository-protection reconciliation contract.

See the preserved contracts, policy, protocol, workflow template, fixtures,
and scope boundary in [`_main/`](_main/).
