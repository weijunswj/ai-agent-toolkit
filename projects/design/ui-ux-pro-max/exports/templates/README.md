# UI/UX Pro Max Template Exports

This project currently does not publish root template outputs.

The placeholder keeps the export directory tracked and makes the empty state explicit. Add curated template exports here only when they are meant to generate or update root-level [templates/](../../../../templates/) surfaces.
